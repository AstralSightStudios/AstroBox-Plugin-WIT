package astrobox:psys-plugin;

interface lifecycle {
    on-load: func();
}

interface event {
    enum event-type {
        plugin-message,
        interconnect-message,
        device-action,
        provider-action,
        deeplink-action
    }

    on-event: func(event-type: event-type, event-payload: string) -> future;
}
